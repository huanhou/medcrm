import { middlewareComposition } from "@/shared/middleware/middleware-composition";
export { configMiddleware } from "@/shared/middleware/config.middleware";

export const middleware = middlewareComposition;
